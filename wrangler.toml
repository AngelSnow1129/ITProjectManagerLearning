# Cloudflare Pages Configuration
# 本项目是纯静态网站，无需构建命令

name = "projectmanager-learning"
compatibility_date = "2026-01-01"

# Pages 项目配置
pages_build_output_dir = "."

# 注意：Cloudflare Pages 会自动部署，无需 wrangler deploy 命令
# 如果使用 Git 集成，推送代码即可自动部署
# 如果使用 CLI 部署，请使用：wrangler pages deploy .
